apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spring-mongo.fullname" . }}-test-connection"
  labels:
    {{- include "spring-mongo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "spring-mongo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
